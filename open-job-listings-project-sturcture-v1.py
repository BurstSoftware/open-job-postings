open-job-listings/
│
├── project-scope.md          <- (This file)
├── app.py                    <- (The main Streamlit Application script)
├── gemini_verifier.py        <- (The verification engine code snippet)
├── requirements.txt          <- (Python dependencies)
├── Dockerfile                <- (Cloud Run deployment manifest)
└── agent_execution_logs.json <- (Created automatically to track compliance)
